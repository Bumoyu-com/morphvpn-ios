# ⚡ MorphProtocol 快速测试配置

## 🖥️ Mac 服务端配置（5分钟）

### 1. 获取 Mac IP
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
# 输出示例: inet 192.168.1.100
```

### 2. 创建 .env 文件
```bash
cd /path/to/morphProtocol
cat > .env << 'EOF'
HOST_NAME=mac-server
HOST_IP=0.0.0.0
HANDSHAKE_PORT_UDP=12301
LOCAL_WG_ADDRESS=127.0.0.1
LOCAL_WG_PORT=51820
PASSWORD=test123
TIMEOUT_DURATION=1200000
TRAFFIC_INTERVAL=600000
HEARTBEAT_INTERVAL=120000
MAX_RETRIES=5
OBFUSCATION_LAYER=3
PADDING_LENGTH=8
LOG_LEVEL=1
EOF
```

### 3. 启动服务端
```bash
npm install
npm run server
```

### 4. 复制加密密钥
服务端启动后会显示：
```
🔐 Encryption Key: XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS
```
**复制这个密钥！**

---

## 📱 iPhone 客户端配置（2分钟）

### 在应用中填写：

| 字段 | 值 | 说明 |
|-----|-----|------|
| **服务器** | `192.168.1.100` | 你的 Mac IP |
| **端口** | `12301` | 默认端口 |
| **加密密钥** | `XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS` | 从服务端复制 |
| **混淆层数** | `3层（推荐）` | 平衡性能和安全 |
| **协议模板** | `QUIC（推荐）` | 伪装成 QUIC 流量 |

### 测试步骤：
1. ✅ 点击 "连接"
2. ✅ 等待状态变为 `connected`
3. ✅ 点击 "发送测试"
4. ✅ 检查是否收到数据

---

## 🔍 快速排查

### 连接失败？
```bash
# 检查 Mac IP
ifconfig | grep "inet " | grep -v 127.0.0.1

# 检查服务端是否运行
ps aux | grep "npm run server"

# 检查端口是否监听
lsof -i :12301
```

### 防火墙问题？
```bash
# macOS 临时关闭防火墙测试
sudo pfctl -d

# 测试完成后重新启用
sudo pfctl -e
```

### 查看日志
```bash
# 服务端日志
npm run server | tee server.log

# 客户端日志（Xcode）
# View → Debug Area → Show Debug Area (⇧⌘Y)
# 搜索: MorphProtocol
```

---

## 📊 参数对照表

| 服务端 (.env) | 客户端 (React) | 默认值 |
|--------------|---------------|--------|
| `HOST_IP` (Mac IP) | 服务器 (host) | `192.168.1.100` |
| `HANDSHAKE_PORT_UDP` | 端口 (port) | `12301` |
| 启动时显示的密钥 | 加密密钥 (encryptionKey) | 动态生成 |
| `OBFUSCATION_LAYER` | 混淆层数 (obfuscationLayer) | `3` |
| `PADDING_LENGTH` | 填充长度 (paddingLength) | `8` |
| - | 协议模板 (templateType) | `1` (QUIC) |

---

## ✅ 成功标志

### 服务端日志应显示：
```
🚀 MorphProtocol Server started
📡 Listening on 0.0.0.0:12301
🔐 Encryption Key: XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS
🤝 Handshake received from 192.168.1.xxx:xxxxx
✅ Client connected: user_xxx
```

### 客户端应显示：
```
✅ 连接成功！
状态: connected
✅ 测试数据已发送
✅ 收到数据: Hello, MorphProtocol! ...
```

---

## 🎯 测试目标

- [ ] 服务端成功启动
- [ ] 客户端成功连接
- [ ] 数据发送成功
- [ ] 数据接收成功
- [ ] 断开连接成功

---

**完整文档**: [MorphProtocol测试指南.md](./MorphProtocol测试指南.md)
