# ✅ 端口切换已实现

## 修改总结

### 问题回顾

**日志分析发现：**
- ✅ IV 长度问题已解决（用户更新了密钥）
- ✅ 握手响应解析完美工作
- ❌ 数据发送到错误端口（12301 而不是 65199）

---

## 实现的修改

### 文件：`MorphUDPClient.swift`

#### 修改 1: 添加新属性 ✅

```swift
class MorphUDPClient {
    private var handshakeConnection: NWConnection?  // 握手连接 (12301)
    private var dataConnection: NWConnection?       // 数据连接 (会话端口)
    private var connection: NWConnection? {         // 兼容性属性
        return dataConnection ?? handshakeConnection
    }
    
    private var host: String = ""
    private var port: UInt16 = 0
    private var sessionPort: UInt16?
    
    // ... 其他属性
}
```

**说明：**
- `handshakeConnection` - 用于握手的连接（12301）
- `dataConnection` - 用于数据传输的连接（会话端口）
- `connection` - 计算属性，优先返回数据连接
- `host`, `port` - 保存服务器地址
- `sessionPort` - 保存会话端口

#### 修改 2: 更新 connect 方法 ✅

```swift
func connect(host: String, port: UInt16) {
    self.host = host
    self.port = port
    
    // 创建握手连接
    handshakeConnection = NWConnection(to: endpoint, using: parameters)
    handshakeConnection?.stateUpdateHandler = { ... }
    handshakeConnection?.start(queue: queue)
}
```

**说明：**
- 保存主机和端口信息
- 创建握手连接而不是通用连接

#### 修改 3: 实现 createDataConnection ✅

```swift
private func createDataConnection(port: UInt16) {
    NSLog("🔌 MorphUDPClient: Creating data connection to port \(port)")
    
    let endpoint = NWEndpoint.hostPort(
        host: NWEndpoint.Host(self.host),
        port: NWEndpoint.Port(rawValue: port)!
    )
    
    let parameters = NWParameters.udp
    parameters.allowLocalEndpointReuse = true
    
    dataConnection = NWConnection(to: endpoint, using: parameters)
    
    dataConnection?.stateUpdateHandler = { [weak self] state in
        switch state {
        case .ready:
            NSLog("✅ MorphUDPClient: Data connection ready on port \(port)")
        case .failed(let error):
            NSLog("❌ MorphUDPClient: Data connection failed: \(error)")
        default:
            break
        }
    }
    
    dataConnection?.start(queue: queue)
}
```

**说明：**
- 创建到会话端口的新连接
- 独立的状态处理
- 自动启动连接

#### 修改 4: 更新 handleHandshakeResponse ✅

```swift
private func handleHandshakeResponse(_ response: [String: Any]) {
    guard let port = response["port"] as? Int else {
        NSLog("❌ Invalid handshake response - missing port")
        return
    }
    
    let status = response["status"] as? String ?? "unknown"
    NSLog("✅ Handshake response - port: \(port), status: \(status)")
    
    // 创建数据连接
    self.sessionPort = UInt16(port)
    createDataConnection(port: UInt16(port))
}
```

**说明：**
- 解析握手响应
- 提取会话端口
- 立即创建数据连接

#### 修改 5: 更新 send 方法 ✅

```swift
func send(_ data: Data) {
    // ... 加密、混淆、封装 ...
    
    // 使用数据连接（如果已建立），否则使用握手连接
    let targetConnection = self.dataConnection ?? self.handshakeConnection
    let connectionType = self.dataConnection != nil ? "data" : "handshake"
    NSLog("📤 Sending to \(connectionType) connection...")
    
    targetConnection?.send(content: packet, ...)
}
```

**说明：**
- 优先使用数据连接
- 如果数据连接未建立，回退到握手连接
- 记录使用的连接类型

#### 修改 6: 更新 disconnect 方法 ✅

```swift
func disconnect() {
    NSLog("🔌 MorphUDPClient: Disconnecting...")
    handshakeConnection?.cancel()
    dataConnection?.cancel()
    handshakeConnection = nil
    dataConnection = nil
    isConnected = false
}
```

**说明：**
- 同时取消两个连接
- 清理所有连接引用

---

## 工作流程

### 连接阶段

```
1. 客户端调用 connect(host: "192.168.0.100", port: 12301)
   ↓
2. 创建 handshakeConnection 到 12301
   ↓
3. 连接就绪，发送握手包
   ↓
4. 接收握手响应 { port: 65199, ... }
   ↓
5. 解析响应，提取端口 65199
   ↓
6. 调用 createDataConnection(port: 65199)
   ↓
7. 创建 dataConnection 到 65199
   ↓
8. 数据连接就绪
```

### 数据发送阶段

```
1. 客户端调用 send(data)
   ↓
2. 加密 → 混淆 → 封装
   ↓
3. 选择连接：dataConnection ?? handshakeConnection
   ↓
4. 发送到 65199（数据连接）
   ↓
5. 服务端在 65199 接收
   ↓
6. 解封装 → 解混淆 → 解密
   ↓
7. 转发到 WireGuard
```

---

## 预期日志

### 客户端日志

**连接阶段：**
```
🔌 MorphUDPClient: Connecting to 192.168.0.100:12301
✅ MorphUDPClient: Connection ready
🤝 MorphUDPClient: Sending handshake...
✅ MorphUDPClient: Handshake sent successfully
📥 MorphUDPClient: Received 108 bytes
🤝 Trying to parse handshake response
✅ Valid handshake response
✅ Handshake response - port: 65199, status: connected
🔌 MorphUDPClient: Creating data connection to port 65199
🔌 MorphUDPClient: Data connection state: ready
✅ MorphUDPClient: Data connection ready on port 65199
```

**数据发送阶段：**
```
📤 MorphUDPClient: Sending 46 bytes
📤 After encrypt: 48 bytes
📤 After obfuscate: 53 bytes
📤 After encapsulate (QUIC): 66 bytes
📤 Sending to data connection...
✅ MorphUDPClient: Sent 66 bytes successfully
```

### 服务端日志

**握手阶段：**
```
[INFO] Received handshake from 192.168.0.102:54772
[INFO] ClientID: bd57b181468e6932ec6b1617bd76c5e6
[INFO] New session socket listening on port 65199
[INFO] Response sent to client
```

**数据接收阶段：**
```
[SESSION-bd57b181] Received packet from 192.168.0.102:54772
[SESSION-bd57b181] Packet size: 66
[SESSION-bd57b181] Before decapsulate: 66 bytes
[SESSION-bd57b181] After decapsulate: 53 bytes
[SESSION-bd57b181] After deobfuscation: 48 bytes
[SESSION-bd57b181] After decryption: 46 bytes
[Client→WG] Packet sent to WireGuard successfully
```

---

## 测试步骤

### 步骤 1: 重新构建

```bash
# 已完成同步
npx cap sync ios

# 在 Xcode 中
Product → Clean Build Folder (⇧⌘K)
Product → Build (⌘B)
Product → Run (⌘R)
```

### 步骤 2: 测试连接

1. 在应用中选择 QUIC 模板
2. 点击连接
3. 查看日志，应该看到：
   ```
   ✅ Handshake response - port: 65199
   🔌 Creating data connection to port 65199
   ✅ Data connection ready on port 65199
   ```

### 步骤 3: 测试数据发送

1. 点击"发送测试"
2. 查看客户端日志：
   ```
   📤 Sending to data connection...
   ✅ Sent 66 bytes successfully
   ```
3. 查看服务端日志：
   ```
   [SESSION-xxx] Received packet from ...
   [SESSION-xxx] After decryption: 46 bytes
   [Client→WG] Packet sent to WireGuard
   ```

### 步骤 4: 验证成功

**成功标志：**
- ✅ 客户端显示"发送到 data connection"
- ✅ 服务端在会话端口（65199）收到数据
- ✅ 数据成功解封装、解混淆、解密
- ✅ 数据转发到 WireGuard

---

## 技术细节

### UDP 连接管理

**为什么需要两个连接？**

虽然 UDP 是无连接协议，但 `NWConnection` 是一个有状态的对象：
- 创建时绑定到特定的目标地址和端口
- 无法动态更改目标端口
- 需要创建新连接来更改目标

**连接生命周期：**
```
handshakeConnection:
  创建 → 握手 → 保持活跃（用于接收响应）

dataConnection:
  握手成功后创建 → 用于所有数据传输
```

### 兼容性设计

**计算属性 `connection`：**
```swift
private var connection: NWConnection? {
    return dataConnection ?? handshakeConnection
}
```

**好处：**
- 向后兼容现有代码
- 自动选择正确的连接
- 简化接收逻辑

---

## 对比：修改前后

### 修改前 ❌

```
客户端:
  创建连接 → 12301
  发送握手 → 12301
  接收响应 ← 12301
  发送数据 → 12301 ❌ (错误！)

服务端:
  12301: 接收握手 ✅
  12301: 接收数据 ❌ (无法识别)
  65199: 等待数据 (永远收不到)
```

### 修改后 ✅

```
客户端:
  创建握手连接 → 12301
  发送握手 → 12301
  接收响应 ← 12301
  创建数据连接 → 65199
  发送数据 → 65199 ✅ (正确！)

服务端:
  12301: 接收握手 ✅
  65199: 接收数据 ✅
  65199: 解密并转发 ✅
```

---

## 总结

### 修改内容

| 修改项 | 状态 | 说明 |
|-------|------|------|
| 添加连接属性 | ✅ | handshakeConnection, dataConnection |
| 保存服务器信息 | ✅ | host, port, sessionPort |
| 更新 connect | ✅ | 创建握手连接 |
| 实现 createDataConnection | ✅ | 创建数据连接 |
| 更新 handleHandshakeResponse | ✅ | 调用 createDataConnection |
| 更新 send | ✅ | 使用数据连接 |
| 更新 disconnect | ✅ | 清理两个连接 |

### 需要的调整

| 位置 | 状态 | 说明 |
|-----|------|------|
| **React 端** | ✅ 无需修改 | 工作正常 |
| **iOS 插件** | ✅ 已完成 | 端口切换已实现 |
| **服务端** | ✅ 无需修改 | 工作正常 |

### 测试清单

- [ ] 在 Xcode 中重新构建
- [ ] 测试连接（查看数据连接创建）
- [ ] 测试数据发送（查看发送到数据连接）
- [ ] 验证服务端接收（查看会话端口接收）
- [ ] 确认数据解密成功

---

**端口切换已完全实现！现在可以测试了。** 🎉

---

**最后更新**: 2024-12-20
**状态**: ✅ 已实现，待测试
