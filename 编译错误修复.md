# 编译错误修复

## 错误原因

PacketTunnelProvider.swift 中调用 MorphUDPClient 的方法与实际实现不匹配。

---

## 已修复的错误

### 1. ✅ MorphUDPClient 初始化参数错误

**错误代码**：
```swift
morphClient = try MorphUDPClient(
    encryptionKey: encryptionKey,
    serverHost: serverHost,        // ❌ 错误：没有这个参数
    serverPort: serverPort,        // ❌ 错误：没有这个参数
    layerCount: layerCount,        // ❌ 错误：参数名不对
    paddingLength: paddingLength
)
```

**正确代码**：
```swift
morphClient = try MorphUDPClient(
    encryptionKey: encryptionKey,
    obfuscationLayer: layerCount,  // ✅ 正确的参数名
    paddingLength: paddingLength
)
```

### 2. ✅ start() 方法不存在

**错误代码**：
```swift
morphClient?.start()  // ❌ 错误：没有 start() 方法
```

**正确代码**：
```swift
morphClient?.connect(host: serverHost, port: UInt16(serverPort))  // ✅ 使用 connect()
```

### 3. ✅ stop() 方法不存在

**错误代码**：
```swift
morphClient.stop()  // ❌ 错误：没有 stop() 方法
```

**正确代码**：
```swift
morphClient.disconnect()  // ✅ 使用 disconnect()
```

### 4. ✅ NWConnection.State 字符串插值错误

**错误代码**：
```swift
NSLog("🔐 MorphProtocol 状态: \(state)")  // ❌ state 不能直接插值
```

**正确代码**：
```swift
let stateStr = String(describing: state)
NSLog("🔐 MorphProtocol 状态: \(stateStr)")  // ✅ 先转换为字符串
```

### 5. ✅ Error 类型插值错误

**错误代码**：
```swift
NSLog("❌ MorphProtocol 错误: \(error)")  // ❌ Error 不能直接插值
```

**正确代码**：
```swift
NSLog("❌ MorphProtocol 错误: \(error.localizedDescription)")  // ✅ 使用 localizedDescription
```

---

## 修复后的完整代码

### PacketTunnelProvider.swift（修复部分）

```swift
// 在 startTunnel 方法中
do {
    morphClient = try MorphUDPClient(
        encryptionKey: encryptionKey,
        obfuscationLayer: layerCount,
        paddingLength: paddingLength
    )
    
    morphClient?.onStateChange = { [weak self] state in
        let stateStr = String(describing: state)
        NSLog("🔐 MorphProtocol 状态: \(stateStr)")
        self?.logger.info("MorphProtocol 状态: \(stateStr)")
    }
    
    morphClient?.onError = { [weak self] error in
        NSLog("❌ MorphProtocol 错误: \(error.localizedDescription)")
        self?.logger.error("MorphProtocol 错误: \(error.localizedDescription)")
    }
    
    morphClient?.onReceive = { [weak self] data in
        NSLog("📥 MorphProtocol 接收数据: \(data.count) 字节")
        self?.logger.debug("MorphProtocol 接收: \(data.count) 字节")
    }
    
    morphClient?.connect(host: serverHost, port: UInt16(serverPort))
    NSLog("✅ MorphProtocol 启动成功")
    logger.info("✅ MorphProtocol 启动成功")
} catch {
    NSLog("❌ 初始化 MorphProtocol 失败: \(error.localizedDescription)")
    logger.error("❌ 初始化 MorphProtocol 失败: \(error.localizedDescription)")
}

// 在 stopTunnel 方法中
if let morphClient = morphClient {
    NSLog("🛑 停止 MorphProtocol")
    logger.info("停止 MorphProtocol")
    morphClient.disconnect()
    self.morphClient = nil
}
```

---

## 验证修复

### 1. 重新编译

在 Xcode 中：
1. 选择 **WireGuardExtension** scheme
2. 按 ⌘B 编译
3. 应该看到：**Build Succeeded** ✅

### 2. 检查错误

所有 5 个错误应该都已解决：
- ✅ 第 94 行：Extra arguments - 已修复
- ✅ 第 95 行：Missing argument - 已修复
- ✅ 第 104 行：CustomStringConvertible - 已修复
- ✅ 第 117 行：No member 'start' - 已修复
- ✅ 第 187 行：No member 'stop' - 已修复

---

## MorphUDPClient API 参考

### 初始化

```swift
init(encryptionKey: String, obfuscationLayer: Int, paddingLength: Int) throws
```

**参数**：
- `encryptionKey`: 加密密钥（格式：`base64key:base64iv`）
- `obfuscationLayer`: 混淆层数（1-4）
- `paddingLength`: 填充长度（1-16）

### 方法

```swift
func connect(host: String, port: UInt16)
func disconnect()
func send(_ data: Data)
```

### 回调

```swift
var onReceive: ((Data) -> Void)?
var onError: ((Error) -> Void)?
var onStateChange: ((NWConnection.State) -> Void)?
```

---

## 如果还有其他错误

### 检查 MorphEncryptor

确保使用正确的初始化方法：
```swift
init(keyString: String) throws
```

### 检查 MorphObfuscator

确保使用正确的初始化方法：
```swift
init(layer: Int = 3, paddingLength: Int = 8)
```

---

## 下一步

修复完成后：

1. ✅ 编译 WireGuardExtension
2. ✅ 编译整个 App
3. ✅ 在真实设备上运行
4. ✅ 测试 MorphProtocol 连接

---

**最后更新**: 2025-12-12  
**状态**: 已修复  
**编译状态**: 应该通过
