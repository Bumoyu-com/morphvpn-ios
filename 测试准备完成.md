# ✅ MorphProtocol 测试准备完成

## 📚 文档清单

已创建以下测试文档：

1. **[MorphProtocol测试指南.md](./MorphProtocol测试指南.md)** - 完整详细指南
   - 服务端配置步骤
   - 客户端配置步骤
   - 参数详解
   - 常见问题排查
   - 调试技巧

2. **[快速测试配置.md](./快速测试配置.md)** - 5分钟快速上手
   - Mac 服务端快速配置
   - iPhone 客户端快速配置
   - 快速排查指南

3. **[参数映射表.md](./参数映射表.md)** - 参数对照表
   - 服务端与客户端参数对应关系
   - 每个参数的详细说明
   - 配置示例
   - 常见错误

---

## 🎯 测试流程概览

### 第一步：Mac 服务端（5分钟）

```bash
# 1. 获取 Mac IP
ifconfig | grep "inet " | grep -v 127.0.0.1

# 2. 进入 morphProtocol 项目
cd /path/to/morphProtocol

# 3. 创建 .env 文件
cp .env.example .env
# 编辑 .env，设置 HOST_IP=0.0.0.0

# 4. 启动服务端
npm install
npm run server

# 5. 复制显示的加密密钥
# 🔐 Encryption Key: XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS
```

### 第二步：iPhone 客户端（2分钟）

在应用的 "MorphProtocol 独立测试" 页面填写：

| 字段 | 值 |
|-----|-----|
| 服务器 | `192.168.1.100` (你的 Mac IP) |
| 端口 | `12301` |
| 加密密钥 | 从服务端复制 |
| 混淆层数 | `3层（推荐）` |
| 协议模板 | `QUIC（推荐）` |

### 第三步：测试连接（1分钟）

1. 点击 "连接"
2. 等待状态变为 `connected`
3. 点击 "发送测试"
4. 检查是否收到数据

---

## 📋 参数快速对照

| 服务端 (.env) | 客户端 (React) | 示例值 |
|--------------|---------------|--------|
| `HOST_IP` (Mac IP) | 服务器 | `192.168.1.100` |
| `HANDSHAKE_PORT_UDP` | 端口 | `12301` |
| 启动时显示 | 加密密钥 | `XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS` |
| `OBFUSCATION_LAYER` | 混淆层数 | `3` |
| `PADDING_LENGTH` | 填充长度 | `8` |
| - | 协议模板 | `1` (QUIC) |

---

## ⚙️ 服务端 .env 最小配置

```bash
HOST_NAME=mac-server
HOST_IP=0.0.0.0
HANDSHAKE_PORT_UDP=12301
LOCAL_WG_ADDRESS=127.0.0.1
LOCAL_WG_PORT=51820
PASSWORD=test123
TIMEOUT_DURATION=1200000
TRAFFIC_INTERVAL=600000
HEARTBEAT_INTERVAL=120000
MAX_RETRIES=5
OBFUSCATION_LAYER=3
PADDING_LENGTH=8
LOG_LEVEL=1
```

---

## 🔍 快速排查

### 问题 1: 无法连接

**检查：**
```bash
# Mac IP 是否正确
ifconfig | grep "inet " | grep -v 127.0.0.1

# 服务端是否运行
ps aux | grep "npm run server"

# 端口是否监听
lsof -i :12301
```

### 问题 2: 连接超时

**检查：**
- Mac 和 iPhone 是否在同一 Wi-Fi
- 防火墙是否阻止端口
- 加密密钥是否正确

**临时关闭防火墙测试：**
```bash
sudo pfctl -d  # 关闭
sudo pfctl -e  # 重新启用
```

### 问题 3: 数据发送失败

**检查：**
- 混淆层数是否匹配
- 查看服务端日志
- 查看 Xcode 控制台日志

---

## 📱 客户端页面位置

### TestMorphProtocol 组件

**文件位置：**
```
src/components/TestMorphProtocol.tsx
```

**使用方式：**
```typescript
import { TestMorphProtocol } from '@/components/TestMorphProtocol';

// 在页面中使用
<TestMorphProtocol />
```

**功能：**
- ✅ 配置连接参数
- ✅ 连接/断开服务器
- ✅ 发送测试数据
- ✅ 接收并显示数据
- ✅ 实时状态监控

---

## 🎓 测试建议

### 基础测试
1. **连接测试** - 验证能否成功连接
2. **数据测试** - 验证能否发送和接收数据
3. **断开测试** - 验证能否正常断开

### 进阶测试
1. **不同混淆层数** - 测试 1-4 层的性能差异
2. **不同协议模板** - 测试 QUIC/KCP/Gaming 的效果
3. **网络切换** - 测试 Wi-Fi 切换时的表现
4. **长时间连接** - 测试连接稳定性

### 性能测试
1. **延迟测试** - 测量往返时间
2. **吞吐量测试** - 测量数据传输速度
3. **丢包率测试** - 测量数据包丢失情况

---

## 📊 成功标志

### 服务端日志
```
🚀 MorphProtocol Server started
📡 Listening on 0.0.0.0:12301
🔐 Encryption Key: XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS
🤝 Handshake received from 192.168.1.xxx:xxxxx
✅ Client connected: user_xxx
📊 Active tunnels: 1
```

### 客户端界面
```
✅ 连接成功！
状态: connected
✅ 测试数据已发送
✅ 收到数据: Hello, MorphProtocol! 2024-12-19T09:00:00.000Z
```

### Xcode 控制台
```
🔌 MorphProtocol: Connecting to 192.168.1.100:12301
🔐 MorphProtocol: Encryption initialized
🤝 MorphProtocol: Handshake sent
✅ MorphProtocol: Connected successfully
📤 MorphProtocol: Data sent
📥 MorphProtocol: Data received
```

---

## 🛠️ 调试工具

### 服务端调试
```bash
# 启用详细日志
LOG_LEVEL=1

# 启用调试模式
DEBUG_MODE=true

# 监听网络流量
sudo tcpdump -i any -n udp port 12301
```

### 客户端调试
```bash
# 在 Xcode 中
# View → Debug Area → Show Debug Area (⇧⌘Y)

# 搜索关键字
MorphProtocol
❌  # 错误
✅  # 成功
```

---

## ✅ 测试检查清单

### 准备阶段
- [ ] morphProtocol 服务端项目已克隆
- [ ] 已安装 Node.js 和依赖
- [ ] 已创建 .env 配置文件
- [ ] Mac 和 iPhone 在同一 Wi-Fi

### 服务端启动
- [ ] 服务端成功启动
- [ ] 显示监听端口
- [ ] 显示加密密钥
- [ ] 无错误日志

### 客户端配置
- [ ] 已填写 Mac IP
- [ ] 已填写端口号
- [ ] 已填写加密密钥
- [ ] 已选择混淆层数
- [ ] 已选择协议模板

### 连接测试
- [ ] 点击连接按钮
- [ ] 状态变为 connected
- [ ] 服务端显示客户端连接
- [ ] 无错误提示

### 数据测试
- [ ] 点击发送测试
- [ ] 显示发送成功
- [ ] 服务端收到数据
- [ ] 客户端收到响应

### 断开测试
- [ ] 点击断开按钮
- [ ] 状态变为 disconnected
- [ ] 服务端显示断开
- [ ] 无错误提示

---

## 🎉 下一步

测试成功后，你可以：

1. **集成到 VPN** - 将 MorphProtocol 与 WireGuard 集成
2. **优化配置** - 调整混淆层数和协议模板
3. **性能测试** - 测试不同配置的性能
4. **生产部署** - 部署到生产环境

---

## 📞 获取帮助

如果遇到问题：

1. **查看文档**
   - [MorphProtocol测试指南.md](./MorphProtocol测试指南.md)
   - [参数映射表.md](./参数映射表.md)

2. **检查日志**
   - 服务端日志
   - Xcode 控制台

3. **常见问题**
   - 网络连接
   - 防火墙设置
   - 参数配置

---

**准备完成！现在可以开始测试了！** 🚀

**最后更新**: 2024-12-19
**状态**: ✅ 就绪
