# 清理完成总结

## 已完成的工作 ✅

### 1. 安装 MorphProtocol 插件 ✅
```bash
cd packages/morphprotocol-plugin
npm install
npm run build
cd ../..
npm install ./packages/morphprotocol-plugin
```

**状态**: ✅ 完成
- 插件已构建
- 已安装到主项目

### 2. 清理 WireGuardExtension ✅
**删除的文件**:
```
ios/App/WireGuardExtension/MorphProtocol/
├── FunctionRegistry.swift
├── MorphEncryptor.swift
├── MorphObfuscator.swift
├── MorphUDPClient.swift
├── ObfuscationFunctions.swift
└── ProtocolTemplates.swift
```

**备份位置**: `ios-morphprotocol-backup-*.tar.gz`

**状态**: ✅ 完成

### 3. 还原 PacketTunnelProvider ✅
**文件**: `ios/App/WireGuardExtension/PacketTunnelProvider.swift`

**状态**: ✅ 完成
- 移除了所有 MorphProtocol 相关代码
- 恢复为纯净的 WireGuard 实现
- 备份文件: `PacketTunnelProvider.swift.backup`

---

## 待完成的步骤

### 步骤 1: 清理 WireGuardPlugin.swift

**文件**: `packages/wireguard-plugin/ios/Plugin/WireGuardPlugin.swift`

**需要移除的代码**:

#### 1.1 移除 MorphProtocol 参数解析（第 56-85 行）
```swift
// 删除这些代码
let useMorphProtocol: Bool = { ... }()
let morphEncryptionKey = call.getString("morphEncryptionKey") ?? ""
let morphServerHost = call.getString("morphServerHost") ?? ""
let morphServerPort = call.getInt("morphServerPort") ?? 0
let morphLayerCount = call.getInt("morphLayerCount") ?? 3
let morphPaddingLength = call.getInt("morphPaddingLength") ?? 8
```

#### 1.2 简化 saveAndConnect 方法调用
```swift
// 修改前
saveAndConnect(
    config: config, 
    tunnelName: tunnelName,
    useMorphProtocol: useMorphProtocol,
    morphEncryptionKey: morphEncryptionKey,
    morphServerHost: morphServerHost,
    morphServerPort: morphServerPort,
    morphLayerCount: morphLayerCount,
    morphPaddingLength: morphPaddingLength
) { success, error in

// 修改后
saveAndConnect(
    config: config, 
    tunnelName: tunnelName
) { success, error in
```

#### 1.3 简化 saveAndConnect 方法定义
```swift
// 修改前
private func saveAndConnect(
    config: String,
    tunnelName: String,
    useMorphProtocol: Bool,
    morphEncryptionKey: String,
    morphServerHost: String,
    morphServerPort: Int,
    morphLayerCount: Int,
    morphPaddingLength: Int,
    completion: @escaping (Bool, Error?) -> Void
)

// 修改后
private func saveAndConnect(
    config: String,
    tunnelName: String,
    completion: @escaping (Bool, Error?) -> Void
)
```

#### 1.4 简化 saveConfiguration 方法
```swift
// 修改前
private func saveConfiguration(
    config: String,
    tunnelName: String,
    useMorphProtocol: Bool,
    morphEncryptionKey: String,
    morphServerHost: String,
    morphServerPort: Int,
    morphLayerCount: Int,
    morphPaddingLength: Int,
    completion: @escaping (Bool, Error?) -> Void
)

// 修改后
private func saveConfiguration(
    config: String,
    tunnelName: String,
    completion: @escaping (Bool, Error?) -> Void
)
```

#### 1.5 移除 providerConfiguration 中的 MorphProtocol 字段
```swift
// 修改前
var providerConfig: [String: NSObject] = [
    "wg_config": config as NSString
]

if useMorphProtocol {
    providerConfig["useMorphProtocol"] = NSNumber(value: true)
    providerConfig["morphEncryptionKey"] = morphEncryptionKey as NSString
    providerConfig["morphServerHost"] = morphServerHost as NSString
    providerConfig["morphServerPort"] = NSNumber(value: morphServerPort)
    providerConfig["morphLayerCount"] = NSNumber(value: morphLayerCount)
    providerConfig["morphPaddingLength"] = NSNumber(value: morphPaddingLength)
}

// 修改后
let providerConfig: [String: NSObject] = [
    "wg_config": config as NSString
]
```

---

### 步骤 2: 清理 TypeScript 类型定义

**文件**: `packages/wireguard-plugin/src/definitions.ts`

**需要移除的字段**:
```typescript
// 删除这些字段
useMorphProtocol?: boolean;
morphEncryptionKey?: string;
morphServerHost?: string;
morphServerPort?: number;
morphLayerCount?: number;
morphPaddingLength?: number;
morphTemplateType?: number;
```

**保留纯净的接口**:
```typescript
export interface WireGuardConnectOptions {
  config: string;
  tunnelName: string;
}

export interface WireGuardSaveConfigOptions {
  config: string;
  tunnelName: string;
}
```

---

### 步骤 3: 创建 React 测试组件

**文件**: `src/components/TestMorphProtocol.tsx`

完整代码见附录 A。

---

### 步骤 4: 更新主应用

#### 4.1 添加到页面

**文件**: `src/pages/VpnPage.tsx`

```typescript
import { TestMorphProtocol } from '../components/TestMorphProtocol';

// 在页面中添加
<TestMorphProtocol />
```

#### 4.2 或创建独立路由

**文件**: `src/App.tsx`

```typescript
import { MorphProtocolTestPage } from './pages/MorphProtocolTestPage';

// 添加路由
<Route path="/morphprotocol-test" element={<MorphProtocolTestPage />} />
```

---

### 步骤 5: 同步和测试

```bash
# 1. 重新构建 WireGuard 插件
cd packages/wireguard-plugin
npm run build
cd ../..

# 2. 同步到 iOS
npx cap sync ios

# 3. 在 Xcode 中打开
npx cap open ios

# 4. 清理并构建
# Product → Clean Build Folder (⇧⌘K)
# Product → Build (⌘B)

# 5. 运行
# Product → Run (⌘R)
```

---

## 附录 A: TestMorphProtocol.tsx 完整代码

```typescript
import React, { useState, useEffect } from 'react';
import { Button, message, Card, Select, Input, Space, Typography } from 'antd';
import { Capacitor } from '@capacitor/core';
import { MorphProtocol } from '@morphvpn/capacitor-morphprotocol';

const { Option } = Select;
const { Title, Text } = Typography;

export const TestMorphProtocol: React.FC = () => {
  const [status, setStatus] = useState<string>('disconnected');
  const [host, setHost] = useState('your-server.com');
  const [port, setPort] = useState(51821);
  const [encryptionKey, setEncryptionKey] = useState('XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS');
  const [layer, setLayer] = useState(3);
  const [padding, setPadding] = useState(8);
  const [templateType, setTemplateType] = useState(1);
  const [receivedData, setReceivedData] = useState<string[]>([]);

  const platform = Capacitor.getPlatform();

  useEffect(() => {
    // 监听状态变化
    MorphProtocol.addListener('statusChanged', (data) => {
      console.log('Status changed:', data);
      setStatus(data.status);
      message.info(`状态: ${data.status}`);
    });

    // 监听接收数据
    MorphProtocol.addListener('dataReceived', (data) => {
      console.log('Data received:', data);
      try {
        const decoded = atob(data.data);
        setReceivedData(prev => [...prev, decoded]);
        message.success(`收到数据: ${decoded.substring(0, 50)}...`);
      } catch (e) {
        console.error('Failed to decode data:', e);
      }
    });

    return () => {
      MorphProtocol.removeAllListeners();
    };
  }, []);

  const handleConnect = async () => {
    try {
      if (platform === 'web') {
        message.warning('MorphProtocol 不支持 Web 平台，请在 iOS 设备上测试', 3);
        return;
      }

      message.loading('正在连接 MorphProtocol...', 0);

      const result = await MorphProtocol.connect({
        host,
        port,
        encryptionKey,
        obfuscationLayer: layer,
        paddingLength: padding,
        templateType,
      });

      message.destroy();

      if (result.success) {
        message.success('连接成功！');
      } else {
        message.error(`连接失败: ${result.message}`);
      }
    } catch (error: any) {
      console.error('Connect failed:', error);
      message.destroy();
      message.error(`连接失败: ${error.message}`);
    }
  };

  const handleDisconnect = async () => {
    try {
      const result = await MorphProtocol.disconnect();
      if (result.success) {
        message.success('已断开连接');
        setStatus('disconnected');
      }
    } catch (error: any) {
      message.error(`断开失败: ${error.message}`);
    }
  };

  const handleSendTest = async () => {
    try {
      const testData = 'Hello, MorphProtocol! ' + new Date().toISOString();
      const base64Data = btoa(testData);

      const result = await MorphProtocol.send({
        data: base64Data,
      });

      if (result.success) {
        message.success('测试数据已发送');
      } else {
        message.error(`发送失败: ${result.message}`);
      }
    } catch (error: any) {
      message.error(`发送失败: ${error.message}`);
    }
  };

  const getStatusColor = () => {
    switch (status) {
      case 'connected': return '#52c41a';
      case 'connecting': return '#1890ff';
      case 'failed': return '#ff4d4f';
      default: return '#d9d9d9';
    }
  };

  return (
    <Card 
      title={
        <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
          <Title level={4} style={{ margin: 0 }}>MorphProtocol 独立测试</Title>
          <div style={{ 
            padding: '4px 12px', 
            background: getStatusColor(),
            color: 'white',
            borderRadius: '4px',
            fontSize: '12px'
          }}>
            {status.toUpperCase()}
          </div>
        </div>
      }
      style={{ margin: '20px' }}
    >
      <Space direction="vertical" style={{ width: '100%' }} size="large">
        {/* 配置区域 */}
        <div>
          <Title level={5}>连接配置</Title>
          <Space direction="vertical" style={{ width: '100%' }}>
            <Input
              addonBefore="服务器地址"
              value={host}
              onChange={(e) => setHost(e.target.value)}
              placeholder="your-server.com"
              disabled={status === 'connected'}
            />
            <Input
              addonBefore="端口"
              type="number"
              value={port}
              onChange={(e) => setPort(Number(e.target.value))}
              disabled={status === 'connected'}
            />
            <Input.TextArea
              rows={2}
              value={encryptionKey}
              onChange={(e) => setEncryptionKey(e.target.value)}
              placeholder="加密密钥 (base64key:base64iv)"
              disabled={status === 'connected'}
            />
            <Select
              style={{ width: '100%' }}
              value={layer}
              onChange={setLayer}
              placeholder="混淆层数"
              disabled={status === 'connected'}
            >
              <Option value={1}>1层 (11种组合)</Option>
              <Option value={2}>2层 (110种组合)</Option>
              <Option value={3}>3层 (990种组合) - 推荐</Option>
              <Option value={4}>4层 (7920种组合)</Option>
            </Select>
            <Select
              style={{ width: '100%' }}
              value={padding}
              onChange={setPadding}
              placeholder="填充长度"
              disabled={status === 'connected'}
            >
              <Option value={4}>4字节</Option>
              <Option value={8}>8字节 - 推荐</Option>
              <Option value={12}>12字节</Option>
              <Option value={16}>16字节</Option>
            </Select>
            <Select
              style={{ width: '100%' }}
              value={templateType}
              onChange={setTemplateType}
              placeholder="协议模板"
              disabled={status === 'connected'}
            >
              <Option value={0}>无模板</Option>
              <Option value={1}>QUIC 协议 - 推荐</Option>
              <Option value={2}>KCP 协议</Option>
              <Option value={3}>游戏协议</Option>
            </Select>
          </Space>
        </div>

        {/* 操作按钮 */}
        <Space>
          <Button 
            type="primary" 
            onClick={handleConnect}
            disabled={status === 'connected' || status === 'connecting'}
            loading={status === 'connecting'}
          >
            连接
          </Button>
          <Button 
            onClick={handleDisconnect}
            disabled={status !== 'connected'}
            danger
          >
            断开
          </Button>
          <Button 
            onClick={handleSendTest}
            disabled={status !== 'connected'}
          >
            发送测试数据
          </Button>
        </Space>

        {/* 接收数据显示 */}
        {receivedData.length > 0 && (
          <div>
            <Title level={5}>接收到的数据 ({receivedData.length})</Title>
            <div style={{ 
              maxHeight: '200px', 
              overflow: 'auto',
              background: '#f5f5f5',
              padding: '10px',
              borderRadius: '4px',
              fontFamily: 'monospace',
              fontSize: '12px'
            }}>
              {receivedData.map((data, index) => (
                <div key={index} style={{ marginBottom: '5px', borderBottom: '1px solid #ddd', paddingBottom: '5px' }}>
                  <Text type="secondary">[{index + 1}]</Text> {data}
                </div>
              ))}
            </div>
          </div>
        )}

        {/* 功能说明 */}
        <div style={{ background: '#f0f2f5', padding: '12px', borderRadius: '4px' }}>
          <Title level={5}>功能说明</Title>
          <ul style={{ margin: 0, paddingLeft: '20px' }}>
            <li>11种混淆函数，动态选择</li>
            <li>3种协议模板，流量伪装</li>
            <li>AES-GCM 加密</li>
            <li>每个数据包使用不同的混淆方式</li>
          </ul>
        </div>

        {/* 平台信息 */}
        <div style={{ fontSize: '12px', color: '#999', textAlign: 'center' }}>
          当前平台: {platform}
          {platform === 'web' && ' (仅支持 iOS 平台)'}
        </div>
      </Space>
    </Card>
  );
};
```

---

## 快速完成命令

```bash
# 1. 同步到 iOS
npx cap sync ios

# 2. 打开 Xcode
npx cap open ios

# 3. 在 Xcode 中:
# - 检查 MorphProtocol 文件夹是否已从 WireGuardExtension 中移除
# - Clean Build Folder (⇧⌘K)
# - Build (⌘B)
# - Run on real device (⌘R)
```

---

## 验证清单

### WireGuard 插件
- [ ] 编译成功
- [ ] 无 MorphProtocol 相关代码
- [ ] VPN 连接正常
- [ ] 只有纯净的 WireGuard 功能

### MorphProtocol 插件
- [ ] 编译成功
- [ ] 独立运行
- [ ] 连接成功
- [ ] 数据发送/接收正常
- [ ] 混淆功能工作
- [ ] 协议模板工作

---

## 预计剩余时间

- 清理 WireGuardPlugin.swift: 30分钟
- 清理 TypeScript 定义: 10分钟
- 创建 React 组件: 30分钟
- 测试验证: 30分钟
- **总计**: 约 1.5-2 小时

---

**文档版本**: 1.0  
**创建时间**: 2025-12-18  
**状态**: 进行中
