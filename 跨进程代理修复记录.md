# MorphProtocol 跨进程代理修复记录

## 问题

iOS 的 Network Extension（WireGuard 运行环境）与 App 是**独立进程**。App 进程中的 `MorphUDPClient` 监听 `127.0.0.1`，但 Extension 进程中的 WireGuard 无法通过 localhost 访问 App 进程的端口。导致 VPN 显示已连接，但实际流量无法通过混淆代理到达服务器，IP 不会改变。

## 解决方案

在 Network Extension 进程内部启动一个轻量级 UDP 代理 `MorphExtensionProxy`，复用 App 进程握手阶段获取的混淆参数，直接在 Extension 内完成混淆/解混淆和数据转发。

```
App 进程:                              Extension 进程:
MorphProtocol 握手 ──(morphConfig)──→  PacketTunnelProvider
  ↓ 获取会话参数                          ├── MorphExtensionProxy
  ↓ substitutionTable                    │   ├── NWListener (本地 UDP)
  ↓ randomValue                          │   ├── MorphObfuscator (相同参数)
  ↓ sessionPort                          │   └── NWConnection → 远程服务器
  ↓ obfuscationKey                       └── WireGuard → 127.0.0.1:proxyPort
  ↓ templateId, clientID
```

## 修改文件清单

### 新增文件

| 文件 | 说明 |
|------|------|
| `ios/App/WireGuardExtension/MorphExtensionProxy.swift` | Extension 进程内的轻量级 UDP 代理，接收 WireGuard 数据包，混淆后转发到远程服务器 |
| `ios/App/WireGuardExtension/MorphObfuscator.swift` | 从 morphprotocol-plugin 复制，供 Extension 编译 |
| `ios/App/WireGuardExtension/ObfuscationFunctions.swift` | 同上 |
| `ios/App/WireGuardExtension/FunctionRegistry.swift` | 同上 |
| `ios/App/WireGuardExtension/ProtocolTemplates.swift` | 同上 |

### 修改文件

| 文件 | 修改内容 |
|------|----------|
| `public/morphVpn_ios.js` | `waitForHandshake()` 返回完整握手数据；`morphConfigForExt` 包含 `fnInitor`（substitutionTable + randomValue） |
| `packages/morphprotocol-plugin/ios/Plugin/MorphProtocolPlugin.swift` | `handshakeComplete` 事件增加 `substitutionTable` 和 `randomValue` 字段 |
| `packages/morphprotocol-plugin/ios/Plugin/MorphUDPClient.swift` | 新增 `substitutionTable` 和 `randomValue` 公共属性 |
| `packages/morphprotocol-plugin/ios/Plugin/MorphObfuscator.swift` | 新增接受外部 `substitutionTable`/`randomValue` 的初始化方法 |
| `packages/morphprotocol-plugin/ios/Plugin/FunctionRegistry.swift` | 新增接受外部参数的初始化方法，透传给 `ObfuscationFunctionRegistry` |
| `packages/morphprotocol-plugin/ios/Plugin/ObfuscationFunctions.swift` | `ObfuscationFunctionRegistry` 新增接受外部参数的初始化方法 |
| `ios/App/WireGuardExtension/PacketTunnelProvider.swift` | 读取 `morph_config`，启动 `MorphExtensionProxy`，替换 WireGuard Endpoint 为本地代理端口 |
| `packages/wireguard-plugin/ios/Plugin/WireGuardPlugin.swift` | `connect()` 接受 `morphConfig` 参数，写入 `providerConfiguration` |
| `packages/wireguard-plugin/src/definitions.ts` | `WireGuardConnectOptions` 增加 `morphConfig?: string` |

## 数据流

1. **App 进程** — `MorphProtocolPlugin.connect()` 启动 `MorphUDPClient`，完成与远程服务器的 AES-256-CBC 加密握手
2. **握手完成** — 触发 `handshakeComplete` 事件，携带 `sessionPort`、`obfuscationKey`、`templateId`、`clientID`、`substitutionTable`、`randomValue`
3. **JS 层** — `morphVpn_ios.js` 将所有参数打包为 `morphConfigForExt` JSON
4. **WireGuard 插件** — 将 `morphConfig` 写入 `NETunnelProviderProtocol.providerConfiguration`
5. **Extension 进程** — `PacketTunnelProvider` 读取 `morph_config`，用相同参数创建 `MorphExtensionProxy`
6. **代理启动** — `MorphExtensionProxy` 在本地随机端口监听 UDP，WireGuard Endpoint 被替换为该端口
7. **数据转发** — WireGuard → 本地代理 → 混淆 + 模板封装 → 远程服务器 → 解封装 + 解混淆 → WireGuard

## fnInitor 参数同步

混淆函数中的 `Substitution`（函数 #9）和 `AddRandomValue`（函数 #10）依赖运行时随机生成的参数：

- `substitutionTable`: 256 字节的随机置换表（Fisher-Yates shuffle）
- `randomValue`: 0-255 的随机值

这些参数在握手阶段发送给服务器。Extension 进程必须使用**完全相同**的参数，否则混淆/解混淆结果不一致，服务器会丢弃数据包。

通过 `morphConfig.fnInitor` 将这两个值从 App 进程传递到 Extension 进程。

## Xcode 构建注意事项

`WireGuardExtension` 目标使用 `PBXFileSystemSynchronizedRootGroup`（Xcode 15+），文件夹内的 `.swift` 文件会自动加入编译。如果 Xcode 未自动识别新增的 4 个文件，需手动将它们添加到 WireGuardExtension target 的 Compile Sources 中。

当 morphprotocol-plugin 中的源文件有更新时，需要同步复制到 `ios/App/WireGuardExtension/` 目录：

```bash
cp packages/morphprotocol-plugin/ios/Plugin/MorphObfuscator.swift ios/App/WireGuardExtension/
cp packages/morphprotocol-plugin/ios/Plugin/ObfuscationFunctions.swift ios/App/WireGuardExtension/
cp packages/morphprotocol-plugin/ios/Plugin/FunctionRegistry.swift ios/App/WireGuardExtension/
cp packages/morphprotocol-plugin/ios/Plugin/ProtocolTemplates.swift ios/App/WireGuardExtension/
```
