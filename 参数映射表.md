# 📋 MorphProtocol 参数映射表

## 完整参数对照

### 连接参数

| 服务端 (.env) | 客户端 (React/iOS) | 类型 | 说明 | 示例值 |
|--------------|-------------------|------|------|--------|
| `HOST_IP` (Mac 本地 IP) | `host` | string | 服务器 IP 地址 | `192.168.1.100` |
| `HANDSHAKE_PORT_UDP` | `port` | number | UDP 端口号 | `12301` |
| 启动时生成并显示 | `encryptionKey` | string | AES 加密密钥 | `XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS` |

### 混淆参数

| 服务端 (.env) | 客户端 (React/iOS) | 类型 | 范围 | 默认值 | 说明 |
|--------------|-------------------|------|------|--------|------|
| `OBFUSCATION_LAYER` | `obfuscationLayer` | number | 1-4 | `3` | 混淆层数，越多越安全 |
| `PADDING_LENGTH` | `paddingLength` | number | 1-16 | `8` | 随机填充长度 |
| - | `templateType` | number | 0-3 | `1` | 协议模板类型 |

### 超时参数（仅服务端）

| 服务端 (.env) | 单位 | 默认值 | 说明 |
|--------------|------|--------|------|
| `TIMEOUT_DURATION` | 毫秒 | `1200000` (20分钟) | 连接超时时间 |
| `TRAFFIC_INTERVAL` | 毫秒 | `600000` (10分钟) | 流量统计间隔 |
| `HEARTBEAT_INTERVAL` | 毫秒 | `120000` (2分钟) | 心跳间隔 |

---

## 参数详解

### 1. host (服务器地址)

**服务端配置：**
```bash
# .env 文件
HOST_IP=0.0.0.0  # 监听所有接口
```

**客户端配置：**
```typescript
// React 组件
host: '192.168.1.100'  // Mac 的实际 IP
```

**获取方式：**
```bash
# 在 Mac 上运行
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**注意事项：**
- ⚠️ 服务端使用 `0.0.0.0` 监听所有接口
- ⚠️ 客户端使用 Mac 的实际 IP 地址
- ⚠️ 不要使用 `127.0.0.1` 或 `localhost`

---

### 2. port (端口号)

**服务端配置：**
```bash
# .env 文件
HANDSHAKE_PORT_UDP=12301
```

**客户端配置：**
```typescript
// React 组件
port: 12301
```

**注意事项：**
- ✅ 必须完全匹配
- ✅ 确保防火墙允许此端口
- ✅ 默认值：`12301`

---

### 3. encryptionKey (加密密钥)

**服务端：**
- 启动时自动生成
- 显示在控制台日志中
- 格式：`base64key:base64iv`

**客户端配置：**
```typescript
// React 组件
encryptionKey: 'XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS'
```

**获取方式：**
```bash
# 服务端启动后查看日志
npm run server
# 输出：
# 🔐 Encryption Key: XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS
```

**注意事项：**
- ⚠️ 每次重启服务端会生成新密钥
- ⚠️ 客户端必须使用最新的密钥
- ⚠️ 密钥包含两部分：`key:iv`

---

### 4. obfuscationLayer (混淆层数)

**服务端配置：**
```bash
# .env 文件
OBFUSCATION_LAYER=3
```

**客户端配置：**
```typescript
// React 组件
obfuscationLayer: 3
```

**可选值：**
- `1`: 最快，安全性最低
- `2`: 较快，安全性中等
- `3`: **推荐** - 平衡性能和安全
- `4`: 最安全，性能略低

**注意事项：**
- ✅ 客户端和服务端必须匹配
- ✅ 推荐使用 `3`

---

### 5. paddingLength (填充长度)

**服务端配置：**
```bash
# .env 文件
PADDING_LENGTH=8
```

**客户端配置：**
```typescript
// React 组件
paddingLength: 8
```

**可选值：**
- `1-4`: 最小填充
- `5-8`: **推荐范围**
- `9-16`: 最大填充

**作用：**
- 防止流量分析
- 隐藏真实数据包大小
- 增加随机性

---

### 6. templateType (协议模板)

**仅客户端配置：**
```typescript
// React 组件
templateType: 1  // QUIC
```

**可选值：**
- `0`: 无模板（原始数据）
- `1`: **QUIC 模板**（推荐）- 伪装成 QUIC 流量
- `2`: KCP 模板 - 伪装成 KCP 流量
- `3`: 游戏协议模板 - 伪装成游戏流量

**作用：**
- 伪装流量特征
- 绕过 DPI 检测
- 提高隐蔽性

---

## React 组件配置示例

### TestMorphProtocol.tsx

```typescript
const [host, setHost] = useState('192.168.1.100');        // Mac IP
const [port, setPort] = useState(12301);                  // 端口
const [encryptionKey, setEncryptionKey] = useState(
  'XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS'
);
const [layer, setLayer] = useState(3);                    // 混淆层数
const [padding, setPadding] = useState(8);                // 填充长度
const [templateType, setTemplateType] = useState(1);      // QUIC 模板

// 连接
await MorphProtocol.connect({
  host,
  port,
  encryptionKey,
  obfuscationLayer: layer,
  paddingLength: padding,
  templateType,
});
```

---

## 服务端 .env 完整示例

```bash
# 服务器配置
HOST_NAME=mac-server
HOST_IP=0.0.0.0                    # 监听所有接口
HANDSHAKE_PORT_UDP=12301           # 客户端连接端口

# WireGuard 配置（可选）
LOCAL_WG_ADDRESS=127.0.0.1
LOCAL_WG_PORT=51820

# 安全配置
PASSWORD=test123

# 超时配置（毫秒）
TIMEOUT_DURATION=1200000           # 20分钟
TRAFFIC_INTERVAL=600000            # 10分钟
HEARTBEAT_INTERVAL=120000          # 2分钟

# 客户端默认配置
MAX_RETRIES=5
OBFUSCATION_LAYER=3                # 混淆层数
PADDING_LENGTH=8                   # 填充长度

# 日志级别
LOG_LEVEL=1                        # 1=详细日志
```

---

## 客户端配置完整示例

```typescript
// 连接配置
const config = {
  host: '192.168.1.100',           // Mac 的本地 IP
  port: 12301,                      // 服务端端口
  encryptionKey: 'XuNgTBIiWFXHSeunT/xPi6DEp98vjw6XBoGogtIJbE8=:4jww75fhLvms4akS',
  obfuscationLayer: 3,              // 混淆层数（1-4）
  paddingLength: 8,                 // 填充长度（1-16）
  templateType: 1,                  // 协议模板（0-3）
};

// 连接
await MorphProtocol.connect(config);
```

---

## 常见配置错误

### ❌ 错误 1: 使用 localhost
```typescript
// 错误
host: 'localhost'  // ❌ 无法从 iPhone 连接

// 正确
host: '192.168.1.100'  // ✅ 使用 Mac 的实际 IP
```

### ❌ 错误 2: 端口不匹配
```bash
# 服务端
HANDSHAKE_PORT_UDP=12301

# 客户端
port: 12302  // ❌ 端口不匹配
```

### ❌ 错误 3: 使用旧密钥
```typescript
// 服务端重启后生成新密钥
// 客户端仍使用旧密钥 ❌

// 解决：从服务端日志复制最新密钥 ✅
```

### ❌ 错误 4: 混淆层数不匹配
```bash
# 服务端
OBFUSCATION_LAYER=3

# 客户端
obfuscationLayer: 2  // ❌ 不匹配
```

---

## 验证配置

### 服务端验证
```bash
# 检查 .env 文件
cat .env | grep -E "HOST_IP|HANDSHAKE_PORT_UDP|OBFUSCATION_LAYER"

# 启动服务端
npm run server

# 确认显示：
# 📡 Listening on 0.0.0.0:12301
# 🔐 Encryption Key: ...
```

### 客户端验证
```typescript
// 在连接前打印配置
console.log('MorphProtocol Config:', {
  host,
  port,
  encryptionKey: encryptionKey.substring(0, 20) + '...',
  obfuscationLayer: layer,
  paddingLength: padding,
  templateType,
});
```

---

## 快速检查清单

配置前：
- [ ] 已获取 Mac 的本地 IP
- [ ] 已创建 .env 文件
- [ ] 已设置正确的端口号

启动后：
- [ ] 服务端成功启动
- [ ] 已复制加密密钥
- [ ] 客户端配置与服务端匹配

连接前：
- [ ] Mac 和 iPhone 在同一 Wi-Fi
- [ ] 防火墙允许端口
- [ ] 所有参数已正确填写

---

**相关文档：**
- [MorphProtocol测试指南.md](./MorphProtocol测试指南.md)
- [快速测试配置.md](./快速测试配置.md)
