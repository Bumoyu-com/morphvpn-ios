# 下一步操作指南

## ✅ 修复已完成

所有代码修复已应用到 `packages/wireguard-plugin/ios/Plugin/WireGuardPlugin.swift`

---

## 🚀 立即执行的步骤

### 步骤 1: 同步到 iOS 项目

```bash
npx cap sync ios
```

**作用**: 将修改后的插件代码同步到 iOS 项目中

---

### 步骤 2: 在 Xcode 中打开项目

```bash
npx cap open ios
```

**作用**: 在 Xcode 中打开项目进行构建

---

### 步骤 3: 清理构建缓存

在 Xcode 中执行:

1. 菜单栏 → **Product** → **Clean Build Folder**
2. 或使用快捷键: **⇧⌘K**

**作用**: 清除旧的编译缓存，确保使用最新代码

---

### 步骤 4: 重新构建项目

在 Xcode 中执行:

1. 菜单栏 → **Product** → **Build**
2. 或使用快捷键: **⌘B**

**等待构建完成**，确保没有编译错误。

---

### 步骤 5: 在真实设备上运行

⚠️ **重要**: VPN 功能只能在真实 iOS 设备上测试，模拟器不支持。

1. 连接 iPhone 到 Mac
2. 在 Xcode 顶部选择你的设备
3. 菜单栏 → **Product** → **Run**
4. 或使用快捷键: **⌘R**

---

### 步骤 6: 测试 VPN 连接

1. 应用启动后，点击 **"Test WireGuard"** 按钮
2. 观察应用内的日志输出
3. 检查 VPN 是否连接成功

---

### 步骤 7: 查看详细日志

在 Mac 上打开 Console.app:

1. 打开 **Console.app**（在 Applications/Utilities 中）
2. 在左侧选择你的 iOS 设备
3. 在搜索框中输入: **WireGuardPlugin**
4. 或搜索: **MorphProtocol**

---

## 📋 验证清单

修复成功的标志（按顺序检查）:

### React 层面（应用日志）
- [ ] ✅ `Configuration validation passed`
- [ ] ✅ `MorphProtocol: 已启用`
- [ ] ✅ `Calling connect with full options...`

### WireGuardPlugin 层面（Console.app）
- [ ] ✅ `useMorphProtocol raw value: 1`
- [ ] ✅ `useMorphProtocol parsed: true`
- [ ] ✅ `MorphProtocol 已启用`
- [ ] ✅ `MorphProtocol 配置已添加到 providerConfig`
- [ ] ✅ `providerConfig keys: ["wg_config", "useMorphProtocol", ...]`
- [ ] ✅ `Saved config keys: ["wg_config", "useMorphProtocol", ...]`

### Network Extension 层面（Console.app）
- [ ] ✅ `MorphProtocol 已启用`
- [ ] ✅ `MorphProtocol 配置: 服务器: morph.example.com:51821`
- [ ] ✅ `MorphUDPClient: Initializing...`
- [ ] ✅ `MorphUDPClient: Initialized with layer=3, padding=8`
- [ ] ✅ `MorphUDPClient: Connecting to morph.example.com:51821`

### VPN 连接
- [ ] ✅ VPN 状态变为 "connected"
- [ ] ✅ 状态栏显示 VPN 图标
- [ ] ✅ 可以访问互联网

---

## 🔍 预期的日志输出

### 成功的日志示例

```
// React 层面
🔍 Validating configuration...
✅ Configuration validation passed
📋 Configuration summary:
隧道名称: MorphVPN
配置长度: 315 字节
MorphProtocol: 已启用
  服务器: morph.example.com:51821
  混淆层数: 3
  填充长度: 8

// WireGuardPlugin 层面
🔵 WireGuardPlugin: connect() called
🔍 WireGuardPlugin: useMorphProtocol raw value: 1
🔍 WireGuardPlugin: useMorphProtocol parsed: true
🔐 WireGuardPlugin: MorphProtocol 已启用
🔐 WireGuardPlugin: 服务器: morph.example.com:51821
🔐 WireGuardPlugin: 混淆层数: 3, 填充: 8
🔐 WireGuardPlugin: 密钥长度: 61 字符
🔐 WireGuardPlugin: MorphProtocol 配置已添加到 providerConfig
🔐 WireGuardPlugin: providerConfig keys: ["wg_config", "useMorphProtocol", "morphEncryptionKey", "morphServerHost", "morphServerPort", "morphLayerCount", "morphPaddingLength"]
🔵 WireGuardPlugin: Provider configuration set
🔵 WireGuardPlugin: Saved config keys: ["wg_config", "useMorphProtocol", ...]
✅ WireGuardPlugin: Configuration saved successfully
✅ WireGuardPlugin: startVPNTunnel() called successfully

// Network Extension 层面
🚀 PacketTunnelProvider: startTunnel() called
📋 No options provided
📦 Getting protocol configuration...
✅ Got protocol configuration
✅ Got WireGuard config, length: 315 bytes
🔐 MorphProtocol 已启用
🔐 MorphProtocol 配置:
   服务器: morph.example.com:51821
   混淆层数: 3
   填充长度: 8
   密钥长度: 61 字符
🔒 MorphUDPClient: Initializing...
✅ MorphUDPClient: Initialized with layer=3, padding=8
🔌 MorphUDPClient: Connecting to morph.example.com:51821
✅ MorphUDPClient: Connection ready
🔧 Parsing WireGuard configuration...
✅ Successfully parsed WireGuard configuration
🚀 Starting WireGuard adapter...
✅ WireGuard tunnel started successfully!
```

---

## ❌ 故障排除

### 问题 1: 仍然显示 "MorphProtocol 未启用"

**症状**:
```
ℹ️ WireGuardPlugin: MorphProtocol 未启用
```

**检查**:
1. 确认文件已保存
2. 确认已执行 `npx cap sync ios`
3. 在 Xcode 中清理构建缓存（⇧⌘K）
4. 重新构建（⌘B）
5. 检查 `useMorphProtocol parsed` 的值

**解决**:
```bash
# 重新同步
npx cap sync ios

# 在 Xcode 中
# 1. Clean Build Folder (⇧⌘K)
# 2. Build (⌘B)
# 3. Run (⌘R)
```

---

### 问题 2: providerConfig keys 不包含 MorphProtocol 参数

**症状**:
```
🔵 WireGuardPlugin: Saved config keys: ["wg_config"]
```

**检查**:
1. 查看是否有 `MorphProtocol 已启用` 的日志
2. 查看 `useMorphProtocol parsed` 的值
3. 检查是否有编译错误

**解决**:
- 确认修改已正确应用
- 查看 Xcode 的编译输出
- 检查是否有语法错误

---

### 问题 3: Network Extension 仍然收不到参数

**症状**:
```
ℹ️ MorphProtocol 未启用，使用标准 WireGuard
```

**可能原因**:
- 旧配置缓存在系统中

**解决方法**:
1. 在 iOS 设置中删除 VPN 配置:
   - 设置 → VPN → 删除 "MorphVPN"
2. 卸载应用
3. 重新安装应用
4. 重新测试连接

---

### 问题 4: 编译错误

**常见错误**:

#### 错误 1: Type mismatch
```
Cannot convert value of type 'String' to expected argument type 'NSString'
```

**解决**: 确保使用 `as NSString` 进行类型转换

#### 错误 2: Missing NSObject
```
Cannot convert value of type 'Bool' to expected dictionary value type 'NSObject'
```

**解决**: 使用 `NSNumber(value: true)` 包装布尔值

---

### 问题 5: VPN 连接失败

**症状**:
- VPN 状态变为 "connecting" 后又回到 "disconnected"

**检查**:
1. MorphProtocol 服务器是否可访问
2. 加密密钥是否正确
3. WireGuard 配置是否正确
4. 网络连接是否正常

**调试**:
- 查看 Console.app 中的错误信息
- 检查 Network Extension 的日志
- 尝试使用标准 WireGuard（不启用 MorphProtocol）

---

## 📊 对比：修复前 vs 修复后

### 修复前
```
❌ useMorphProtocol parsed: false
❌ providerConfig keys: ["wg_config"]
❌ ℹ️ MorphProtocol 未启用，使用标准 WireGuard
```

### 修复后
```
✅ useMorphProtocol parsed: true
✅ providerConfig keys: ["wg_config", "useMorphProtocol", "morphEncryptionKey", ...]
✅ 🔐 MorphProtocol 已启用
```

---

## 📞 需要帮助？

如果遇到问题，请提供以下信息：

1. **Console.app 的完整日志**
   - 搜索 "WireGuardPlugin"
   - 搜索 "MorphProtocol"
   - 搜索 "PacketTunnelProvider"

2. **Xcode 的编译输出**
   - 是否有编译错误或警告

3. **VPN 连接状态**
   - 是否显示 VPN 图标
   - 连接状态是什么

4. **测试环境**
   - iOS 版本
   - 设备型号
   - Xcode 版本

---

## 📚 相关文档

- [修复已应用.md](./修复已应用.md) - 修复详情
- [日志分析报告.md](./日志分析报告.md) - 问题分析
- [测试结果总结.md](./测试结果总结.md) - 测试结果
- [快速参考_MorphProtocol配置.md](./快速参考_MorphProtocol配置.md) - 配置参考

---

## ✅ 成功标准

当你看到以下所有内容时，说明修复成功：

1. ✅ `useMorphProtocol parsed: true`
2. ✅ `MorphProtocol 已启用`
3. ✅ `MorphProtocol 配置已添加到 providerConfig`
4. ✅ `providerConfig keys` 包含所有 MorphProtocol 参数
5. ✅ Network Extension 显示 `MorphProtocol 已启用`
6. ✅ `MorphUDPClient: Initializing...`
7. ✅ VPN 连接成功

---

**祝测试顺利！** 🎉

如有问题，请随时提供日志以便进一步分析。
