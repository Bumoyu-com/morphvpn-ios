# 🚀 批量测试工具集成指南

## 快速开始

### 步骤 1: 文件已创建

以下文件已自动创建：

```
src/components/
├── MorphProtocolBatchTest.tsx    # 批量测试组件
├── MorphProtocolTestHub.tsx      # 测试中心（包含单次和批量测试）
└── TestMorphProtocol.tsx          # 原有的单次测试组件
```

### 步骤 2: 在路由中添加

在你的路由配置文件中添加测试页面：

**方式 1: 添加独立路由**

```typescript
// src/App.tsx 或路由配置文件
import { MorphProtocolTestHub } from './components/MorphProtocolTestHub';

// 在路由中添加
<Route path="/morph-test" element={<MorphProtocolTestHub />} />
```

**方式 2: 添加到现有页面**

```typescript
import { MorphProtocolTestHub } from './components/MorphProtocolTestHub';

function YourPage() {
  return (
    <div>
      <MorphProtocolTestHub />
    </div>
  );
}
```

### 步骤 3: 访问测试页面

在浏览器或应用中访问：
```
http://localhost:5173/morph-test
```

或者在你的应用菜单中添加链接。

---

## 组件说明

### MorphProtocolTestHub

**功能**: 测试中心，包含两个标签页
- 🔧 单次测试 - 原有的单次测试功能
- 🧪 批量测试 - 新的批量测试功能

**使用**:
```typescript
import { MorphProtocolTestHub } from '@/components/MorphProtocolTestHub';

<MorphProtocolTestHub />
```

### MorphProtocolBatchTest

**功能**: 批量测试组件
- 选择测试配置
- 自动批量测试
- 显示测试结果
- 导出 CSV 报告

**使用**:
```typescript
import { MorphProtocolBatchTest } from '@/components/MorphProtocolBatchTest';

<MorphProtocolBatchTest />
```

### TestMorphProtocol

**功能**: 单次测试组件（原有功能）
- 手动配置参数
- 单次连接测试
- 发送测试数据

**使用**:
```typescript
import { TestMorphProtocol } from '@/components/TestMorphProtocol';

<TestMorphProtocol />
```

---

## 功能特性

### 批量测试功能

✅ **灵活配置**
- 选择混淆层数（1-4层）
- 选择填充长度（4-16字节）
- 选择协议模板（None/QUIC/KCP/Gaming）

✅ **自动测试**
- 自动生成测试组合
- 依次执行测试
- 实时显示进度

✅ **结果分析**
- 成功率统计
- 平均耗时
- 详细结果表格

✅ **数据导出**
- 导出 CSV 格式
- 可在 Excel 中分析

---

## 使用示例

### 示例 1: 测试推荐配置

```typescript
// 在批量测试页面中：
// 1. 配置服务器
服务器: 192.168.0.100
端口: 12301
加密密钥: (从服务器获取)

// 2. 选择配置
混淆层数: ☑️ 3层
填充长度: ☑️ 8字节
协议模板: ☑️ QUIC

// 3. 点击"开始批量测试"
// 结果: 测试 1 个配置
```

### 示例 2: 全面测试

```typescript
// 1. 配置服务器（同上）

// 2. 选择所有配置
混淆层数: ☑️ 1层, 2层, 3层, 4层
填充长度: ☑️ 4, 8, 12, 16字节
协议模板: ☑️ None, QUIC, KCP, Gaming

// 3. 点击"开始批量测试"
// 结果: 测试 4 × 4 × 4 = 64 个配置
```

### 示例 3: 对比测试

```typescript
// 对比不同混淆层数的性能

// 1. 配置服务器（同上）

// 2. 选择配置
混淆层数: ☑️ 全选
填充长度: ☑️ 8字节
协议模板: ☑️ QUIC

// 3. 点击"开始批量测试"
// 结果: 测试 4 个配置，对比性能
```

---

## 测试结果示例

### 统计信息

```
总测试数: 16
成功: 14
失败: 2
成功率: 87.5%
平均耗时: 1850ms
```

### 详细结果

| ID | 层数 | 填充 | 模板 | 状态 | 耗时 | 错误 |
|----|------|------|------|------|------|------|
| L1-P8-T1 | 1 | 8 | QUIC | ✅ 成功 | 1200ms | - |
| L2-P8-T1 | 2 | 8 | QUIC | ✅ 成功 | 1600ms | - |
| L3-P8-T1 | 3 | 8 | QUIC | ✅ 成功 | 2000ms | - |
| L4-P8-T1 | 4 | 8 | QUIC | ❌ 失败 | 2500ms | Connection timeout |

---

## 导出的 CSV 格式

```csv
ID,Layer,Padding,Template,Status,Duration(ms),Error,Timestamp
L1-P8-T1,1,8,QUIC,success,1200,,2024-12-23T10:30:00.000Z
L2-P8-T1,2,8,QUIC,success,1600,,2024-12-23T10:30:05.000Z
L3-P8-T1,3,8,QUIC,success,2000,,2024-12-23T10:30:10.000Z
L4-P8-T1,4,8,QUIC,failed,2500,Connection timeout,2024-12-23T10:30:15.000Z
```

---

## 自定义配置

### 修改默认服务器配置

在 `MorphProtocolBatchTest.tsx` 中修改：

```typescript
const [host, setHost] = useState('YOUR_SERVER_IP');
const [port, setPort] = useState(YOUR_PORT);
const [encryptionKey, setEncryptionKey] = useState('YOUR_KEY');
```

### 修改默认测试选项

```typescript
const [selectedLayers, setSelectedLayers] = useState<number[]>([3]); // 只测试 3 层
const [selectedPaddings, setSelectedPaddings] = useState<number[]>([8]); // 只测试 8 字节
const [selectedTemplates, setSelectedTemplates] = useState<number[]>([1]); // 只测试 QUIC
```

### 修改测试等待时间

```typescript
// 在 runSingleTest 函数中
await new Promise(resolve => setTimeout(resolve, 2000)); // 连接等待时间
await new Promise(resolve => setTimeout(resolve, 500));  // 断开等待时间
```

---

## 添加到导航菜单

### 在侧边栏添加链接

```typescript
// src/components/Sidebar.tsx
import { ExperimentOutlined } from '@ant-design/icons';

<Menu.Item key="morph-test" icon={<ExperimentOutlined />}>
  <Link to="/morph-test">MorphProtocol 测试</Link>
</Menu.Item>
```

### 在顶部菜单添加

```typescript
// src/components/Header.tsx
<Menu.Item key="morph-test">
  <Link to="/morph-test">批量测试</Link>
</Menu.Item>
```

---

## 故障排查

### 问题 1: 组件无法导入

**错误**: `Cannot find module '@/components/MorphProtocolTestHub'`

**解决**:
```typescript
// 使用相对路径
import { MorphProtocolTestHub } from './components/MorphProtocolTestHub';

// 或配置路径别名
// vite.config.ts
resolve: {
  alias: {
    '@': path.resolve(__dirname, './src')
  }
}
```

### 问题 2: 测试无法启动

**错误**: `MorphProtocol 不支持 Web 平台`

**解决**: 在 iOS 设备或模拟器上测试，Web 平台不支持原生插件。

### 问题 3: 所有测试都失败

**检查**:
1. 服务器是否运行
2. 网络是否连接
3. 加密密钥是否正确
4. 防火墙是否阻止

---

## 性能优化

### 减少测试时间

```typescript
// 减少等待时间（可能影响准确性）
await new Promise(resolve => setTimeout(resolve, 1000)); // 从 2000 改为 1000
```

### 并行测试（高级）

```typescript
// 注意：可能导致资源竞争
const results = await Promise.all(
  configs.map(config => runSingleTest(config))
);
```

---

## 扩展功能

### 添加数据发送测试

在 `runSingleTest` 中添加：

```typescript
// 连接后发送测试数据
await MorphProtocol.send({
  data: btoa('Test data')
});

// 等待响应
await new Promise(resolve => setTimeout(resolve, 1000));
```

### 添加重试机制

```typescript
const runSingleTestWithRetry = async (config: TestConfig, retries = 3) => {
  for (let i = 0; i < retries; i++) {
    const result = await runSingleTest(config);
    if (result.status === 'success') {
      return result;
    }
  }
  return result; // 返回最后一次失败结果
};
```

### 添加实时日志

```typescript
const [logs, setLogs] = useState<string[]>([]);

// 在测试中添加日志
setLogs(prev => [...prev, `Testing ${testId}...`]);
```

---

## 总结

### 已创建的文件

1. ✅ `MorphProtocolBatchTest.tsx` - 批量测试组件
2. ✅ `MorphProtocolTestHub.tsx` - 测试中心
3. ✅ `批量测试工具使用指南.md` - 使用文档
4. ✅ `批量测试工具集成指南.md` - 本文档

### 下一步

1. **集成到应用**
   - 添加路由
   - 添加菜单链接

2. **开始测试**
   - 配置服务器
   - 选择测试配置
   - 运行批量测试

3. **分析结果**
   - 查看统计信息
   - 导出 CSV
   - 找出最优配置

---

**开始使用批量测试工具，找到最适合你的配置！** 🚀

---

**最后更新**: 2024-12-23
**版本**: 1.0.0
