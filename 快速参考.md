# 🚀 快速参考

## 📦 插件安装

```bash
# 安装所有插件
./INSTALL_PLUGIN.sh all

# 安装单个插件
./INSTALL_PLUGIN.sh wireguard
./INSTALL_PLUGIN.sh morphprotocol
```

## 🧪 测试插件

```bash
# 测试所有插件
./TEST_PLUGINS.sh
```

## 🔨 构建插件

```bash
# WireGuard 插件
cd packages/wireguard-plugin
npm install
npm run build

# MorphProtocol 插件
cd packages/morphprotocol-plugin
npm install
npm run build
```

## 📱 在 Xcode 中测试

```bash
# 1. 打开项目
npx cap open ios

# 2. 在 Xcode 中
# - Product → Clean Build Folder (⇧⌘K)
# - Product → Build (⌘B)
# - Product → Run (⌘R)
```

## 💻 代码示例

### WireGuard
```typescript
import { WireGuard } from '@morphvpn/capacitor-wireguard';

await WireGuard.connect({
  config: wireguardConfig,
  tunnelName: 'MyVPN'
});
```

### MorphProtocol
```typescript
import { MorphProtocol } from '@morphvpn/capacitor-morphprotocol';

await MorphProtocol.connect({
  host: 'server.com',
  port: 51821,
  encryptionKey: 'key:iv',
  obfuscationLayer: 3,
  paddingLength: 8,
  templateType: 1  // QUIC
});
```

## 📂 文件位置

### WireGuard 插件
- 源码: `packages/wireguard-plugin/`
- iOS: `packages/wireguard-plugin/ios/Plugin/`
- 定义: `packages/wireguard-plugin/src/definitions.ts`

### MorphProtocol 插件
- 源码: `packages/morphprotocol-plugin/`
- iOS: `packages/morphprotocol-plugin/ios/Plugin/`
- 定义: `packages/morphprotocol-plugin/src/definitions.ts`

## ✅ 检查清单

- [ ] 运行 `./TEST_PLUGINS.sh` 确认构建成功
- [ ] 运行 `./INSTALL_PLUGIN.sh all` 安装插件
- [ ] 运行 `npx cap sync ios` 同步到 iOS
- [ ] 在 Xcode 中清理构建文件夹
- [ ] 在 Xcode 中构建项目
- [ ] 在真实设备上测试

## 🆘 常见问题

### 构建失败
```bash
# 清理并重新构建
cd packages/wireguard-plugin
npm run clean
npm install
npm run build
```

### 插件未识别
```bash
# 重新同步
npx cap sync ios
```

### Xcode 错误
```bash
# 清理 Xcode 缓存
# Product → Clean Build Folder (⇧⌘K)
```

## 📚 完整文档

- [插件清理和独立化完成报告.md](./插件清理和独立化完成报告.md)
- [集成完成总结.txt](./集成完成总结.txt)
- [清理完成总结.md](./清理完成总结.md)
