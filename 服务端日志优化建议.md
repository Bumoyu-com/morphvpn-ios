# 🔍 服务端日志优化建议

## 问题分析

### 当前问题
1. **发送测试无效** - 服务端无法识别数据包
2. **KCP 模板崩溃** - 切换到 KCP 模板后程序崩溃

---

## 服务端日志优化

### 1. 增强解密错误日志

**当前代码：**
```typescript
try {
  decryptedMessage = Buffer.from(encryptor.simpleDecrypt(message.toString()));
} catch (e) {
  logger.debug(`type: ${typeof message}, str:${message.toString()}`);
  logger.debug(`Received non-control message on main server port from ${remote.address}:${remote.port}`);
  return;
}
```

**优化后的代码：**
```typescript
try {
  // 详细记录接收到的原始数据
  logger.info(`[RAW] Received ${message.length} bytes from ${remote.address}:${remote.port}`);
  logger.info(`[RAW] Type: ${typeof message}`);
  logger.info(`[RAW] First 50 bytes (hex): ${message.slice(0, 50).toString('hex')}`);
  logger.info(`[RAW] First 50 bytes (base64): ${message.slice(0, 50).toString('base64')}`);
  logger.info(`[RAW] As string (first 100 chars): ${message.toString().substring(0, 100)}`);
  
  // 尝试解密
  const messageString = message.toString();
  logger.info(`[DECRYPT] Attempting to decrypt ${messageString.length} chars`);
  logger.info(`[DECRYPT] String preview: ${messageString.substring(0, 100)}...`);
  
  decryptedMessage = Buffer.from(encryptor.simpleDecrypt(messageString));
  
  logger.info(`[DECRYPT] Success! Decrypted ${decryptedMessage.length} bytes`);
  logger.info(`[DECRYPT] Decrypted content: ${decryptedMessage.toString()}`);
  
} catch (e) {
  // 详细的错误信息
  logger.error(`[DECRYPT] Failed to decrypt message from ${remote.address}:${remote.port}`);
  logger.error(`[DECRYPT] Error: ${e.message}`);
  logger.error(`[DECRYPT] Error stack: ${e.stack}`);
  logger.error(`[DECRYPT] Message length: ${message.length}`);
  logger.error(`[DECRYPT] Message type: ${typeof message}`);
  logger.error(`[DECRYPT] Message hex (first 100): ${message.slice(0, 100).toString('hex')}`);
  logger.error(`[DECRYPT] Message base64 (first 100): ${message.slice(0, 100).toString('base64')}`);
  
  // 尝试判断是否是数据包
  const ipKey = `${remote.address}:${remote.port}`;
  const possibleClientIDs = Array.from(ipIndex.entries())
    .filter(([key]) => key.startsWith(ipKey))
    .map(([key, clientID]) => ({ key, clientID }));
  
  if (possibleClientIDs.length > 0) {
    logger.info(`[DECRYPT] Found ${possibleClientIDs.length} possible sessions for this IP:port`);
    possibleClientIDs.forEach(({ key, clientID }) => {
      logger.info(`[DECRYPT]   - Key: ${key}, ClientID: ${clientID}`);
    });
  } else {
    logger.warn(`[DECRYPT] No active session found for ${remote.address}:${remote.port}`);
    logger.warn(`[DECRYPT] This might be a data packet sent to wrong port`);
  }
  
  return;
}
```

### 2. 增强会话查找日志

在数据包处理部分添加：

```typescript
// 在 "Handle incoming messages" 部分
server.on('message', async (message, remote) => {
  logger.debug(`[INCOMING] Packet from ${remote.address}:${remote.port}, size: ${message.length}`);
  
  // Rate limit check
  if (!packetRateLimiter.isAllowed(remote.address)) {
    logger.warn(`[RATE-LIMIT] Packet rate limit exceeded for ${remote.address}`);
    return;
  }
  
  // ... 解密逻辑 ...
});
```

### 3. 增强会话数据包处理日志

在 `newSocket.on('message', ...)` 中添加：

```typescript
newSocket.on('message', (wgMessage, wgRemote) => {
  logger.debug(`[SESSION-${clientID}] Received packet from ${wgRemote.address}:${wgRemote.port}`);
  logger.debug(`[SESSION-${clientID}] Packet size: ${wgMessage.length}`);
  logger.debug(`[SESSION-${clientID}] Packet hex (first 50): ${wgMessage.slice(0, 50).toString('hex')}`);
  
  // 检查是否来自 WireGuard
  if (wgRemote.address === LOCALWG_ADDRESS && wgRemote.port === LOCALWG_PORT) {
    logger.debug(`[SESSION-${clientID}] ✅ From WireGuard`);
    // ... WireGuard 处理逻辑 ...
  } else {
    logger.debug(`[SESSION-${clientID}] ✅ From client`);
    
    // 记录解封装前的数据
    logger.info(`[SESSION-${clientID}] Before decapsulate: ${wgMessage.length} bytes`);
    logger.info(`[SESSION-${clientID}] Hex: ${wgMessage.slice(0, 50).toString('hex')}`);
    
    const obfuscatedData = session.template.decapsulate(wgMessage);
    
    if (!obfuscatedData) {
      logger.error(`[SESSION-${clientID}] ❌ Decapsulation failed!`);
      logger.error(`[SESSION-${clientID}] Template: ${session.template.name}`);
      logger.error(`[SESSION-${clientID}] Packet size: ${wgMessage.length}`);
      logger.error(`[SESSION-${clientID}] Packet hex: ${wgMessage.toString('hex')}`);
      return;
    }
    
    logger.info(`[SESSION-${clientID}] After decapsulate: ${obfuscatedData.length} bytes`);
    
    // ... 继续处理 ...
  }
});
```

### 4. 添加握手响应日志

```typescript
// 在发送握手响应时
const response = { port: newPort, clientID: clientIDBase64, status: 'connected' };
const responseEncrypted = encryptor.simpleEncrypt(JSON.stringify(response));

logger.info(`[HANDSHAKE] Sending response to ${remote.address}:${remote.port}`);
logger.info(`[HANDSHAKE] Response JSON: ${JSON.stringify(response)}`);
logger.info(`[HANDSHAKE] Encrypted length: ${responseEncrypted.length}`);
logger.info(`[HANDSHAKE] Encrypted preview: ${responseEncrypted.substring(0, 100)}...`);

server.send(responseEncrypted, 0, responseEncrypted.length, remote.port, remote.address, (error) => {
  if (error) {
    logger.error(`[HANDSHAKE] ❌ Failed to send response: ${error.message}`);
  } else {
    logger.info(`[HANDSHAKE] ✅ Response sent successfully`);
  }
});
```

---

## 客户端问题分析

### 问题 1: 发送测试无效

**可能原因：**

1. **数据发送到错误的端口**
   - 握手后，服务端会分配一个新端口
   - 客户端需要切换到新端口发送数据
   - 当前实现可能仍在使用握手端口

2. **数据格式不正确**
   - 数据需要经过：加密 → 混淆 → 协议封装
   - 可能某个步骤出错

3. **会话未建立**
   - 握手可能没有完全成功
   - 服务端可能没有创建会话

**检查点：**

```swift
// 在 MorphUDPClient.swift 的 send 方法中添加日志
func send(_ data: Data) {
    guard isConnected else {
        NSLog("❌ MorphUDPClient: Not connected, cannot send")
        return
    }
    
    NSLog("📤 MorphUDPClient: Sending \(data.count) bytes")
    NSLog("📤 Original data (hex): \(data.prefix(50).map { String(format: "%02x", $0) }.joined(separator: " "))")
    
    queue.async { [weak self] in
        guard let self = self else { return }
        
        do {
            // 1. 加密
            let encrypted = try self.encryptor.encrypt(data)
            NSLog("📤 After encrypt: \(encrypted.count) bytes")
            NSLog("📤 Encrypted (hex): \(encrypted.prefix(50).map { String(format: "%02x", $0) }.joined(separator: " "))")
            
            // 2. 混淆
            let obfuscated = self.obfuscator.obfuscate(encrypted)
            NSLog("📤 After obfuscate: \(obfuscated.count) bytes")
            NSLog("📤 Obfuscated (hex): \(obfuscated.prefix(50).map { String(format: "%02x", $0) }.joined(separator: " "))")
            
            // 3. 协议封装
            let packet: Data
            if let template = self.template {
                packet = template.encapsulate(obfuscated, clientID: self.clientID)
                NSLog("📤 After encapsulate (\(template.name)): \(packet.count) bytes")
                NSLog("📤 Final packet (hex): \(packet.prefix(50).map { String(format: "%02x", $0) }.joined(separator: " "))")
            } else {
                packet = obfuscated
                NSLog("📤 No template, using obfuscated data directly")
            }
            
            // 4. 发送
            NSLog("📤 Sending to connection...")
            self.connection?.send(content: packet, completion: .contentProcessed { error in
                if let error = error {
                    NSLog("❌ Send error: \(error)")
                } else {
                    NSLog("✅ Sent \(packet.count) bytes successfully")
                }
            })
            
        } catch {
            NSLog("❌ Processing error: \(error)")
        }
    }
}
```

### 问题 2: KCP 模板崩溃

**可能原因：**

1. **KCP 模板实现有 bug**
   - 数组越界
   - 空指针访问
   - 类型转换错误

2. **ClientID 长度不匹配**
   - KCP 使用 4 字节 headerID
   - QUIC 使用 8 字节 headerID
   - 可能在提取 headerID 时出错

**检查 KCP 模板实现：**

```swift
// 在 ProtocolTemplates.swift 中检查 KcpTemplate
class KcpTemplate: ProtocolTemplate {
    let id: UInt8 = 2
    let templateID: UInt8 = 2
    let name = "KCP"
    
    func encapsulate(_ data: Data, clientID: Data) -> Data {
        NSLog("🎭 KCP: Encapsulating \(data.count) bytes")
        NSLog("🎭 KCP: ClientID length: \(clientID.count)")
        
        // 确保 clientID 至少有 4 字节
        guard clientID.count >= 4 else {
            NSLog("❌ KCP: ClientID too short (\(clientID.count) bytes)")
            return data  // 返回原始数据，避免崩溃
        }
        
        var packet = Data()
        
        // KCP 头部
        packet.append(0x01)  // conv (4 bytes) - 使用 clientID 前 4 字节
        packet.append(clientID.prefix(4))
        
        NSLog("🎭 KCP: Header added, total: \(packet.count) bytes")
        
        // 添加数据
        packet.append(data)
        
        NSLog("🎭 KCP: Final packet: \(packet.count) bytes")
        return packet
    }
    
    func decapsulate(_ packet: Data) -> Data? {
        NSLog("🎭 KCP: Decapsulating \(packet.count) bytes")
        
        // KCP 头部至少 5 字节
        guard packet.count > 5 else {
            NSLog("❌ KCP: Packet too short (\(packet.count) bytes)")
            return nil
        }
        
        // 跳过头部（1 + 4 = 5 字节）
        let data = packet.dropFirst(5)
        
        NSLog("🎭 KCP: Extracted \(data.count) bytes")
        return Data(data)
    }
}
```

---

## 测试步骤

### 1. 启用详细日志

**服务端：**
```bash
# 在 .env 中
LOG_LEVEL=1
DEBUG_MODE=true
```

**客户端：**
在 Xcode 中查看完整日志

### 2. 测试握手

1. 连接服务器
2. 查看服务端日志：
   ```
   [HANDSHAKE] Received handshake from ...
   [HANDSHAKE] ClientID: ...
   [HANDSHAKE] Response sent successfully
   ```
3. 查看客户端日志：
   ```
   ✅ MorphUDPClient: Handshake sent successfully
   📥 MorphUDPClient: Received ... bytes
   ```

### 3. 测试发送数据

1. 点击 "发送测试"
2. 查看客户端日志：
   ```
   📤 MorphUDPClient: Sending ... bytes
   📤 After encrypt: ... bytes
   📤 After obfuscate: ... bytes
   📤 After encapsulate: ... bytes
   ✅ Sent ... bytes successfully
   ```
3. 查看服务端日志：
   ```
   [SESSION-xxx] Received packet from ...
   [SESSION-xxx] Before decapsulate: ... bytes
   [SESSION-xxx] After decapsulate: ... bytes
   [SESSION-xxx] After deobfuscation: ... bytes
   ```

### 4. 测试 KCP 模板

1. 切换到 KCP 模板
2. 点击连接
3. 查看是否崩溃
4. 如果崩溃，查看崩溃日志

---

## 预期日志输出

### 成功的数据发送

**客户端：**
```
📤 MorphUDPClient: Sending 23 bytes
📤 Original data (hex): 48 65 6c 6c 6f 2c 20 4d 6f 72 70 68 50 72 6f 74 6f 63 6f 6c 21 20 32
📤 After encrypt: 48 bytes
📤 Encrypted (hex): a3 f2 8d 1c 9e 4b 7a 2f ...
📤 After obfuscate: 54 bytes
📤 Obfuscated (hex): 7f 3a c1 8d 2e 9f 4b ...
📤 After encapsulate (QUIC): 67 bytes
📤 Final packet (hex): 40 12 34 56 78 9a bc de ...
✅ Sent 67 bytes successfully
```

**服务端：**
```
[SESSION-1234...] Received packet from 192.168.0.102:54321
[SESSION-1234...] Packet size: 67
[SESSION-1234...] Before decapsulate: 67 bytes
[SESSION-1234...] After decapsulate: 54 bytes
[SESSION-1234...] After deobfuscation: 48 bytes
[Client→WG] Packet sent to WireGuard successfully
```

---

## 总结

### 服务端优化
1. ✅ 添加详细的原始数据日志
2. ✅ 添加解密过程日志
3. ✅ 添加会话查找日志
4. ✅ 添加数据包处理日志

### 客户端检查
1. ✅ 检查端口切换逻辑
2. ✅ 添加详细的发送日志
3. ✅ 修复 KCP 模板崩溃

### 下一步
1. 应用服务端日志优化
2. 重新测试发送功能
3. 修复发现的问题
4. 测试 KCP 模板

---

**最后更新**: 2024-12-19
**状态**: 📝 待测试
